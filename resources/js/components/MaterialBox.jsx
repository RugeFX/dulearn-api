import React from "react";

const MaterialBox = ({ title, desc }) => {
    return (
        <div>
            <p>{title}</p>
            <p>{desc}</p>
        </div>
    );
};

export default MaterialBox;
